
/* 
check the color of headset if it match to the user choice
If the requirements is meet then connect the headset to our app
store the headset properties to the local storage.
*/

const synchronize_headset = () => {
    
}

export {
    synchronize_headset
}